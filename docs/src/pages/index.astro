---
// English landing (default locale, served at /noteflow/). The whole page lives in
// Home.astro so it can be reused for every locale — see src/pages/es/index.astro.
import Home from '../components/Home.astro';
---

<Home lang="en" />
