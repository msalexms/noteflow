---
// English terms of service (default locale, served at /noteflow/terms/). The whole
// page lives in TermsPage.astro so it can be reused per locale — see
// src/pages/es/terms.astro.
import TermsPage from '../components/docs/TermsPage.astro';
---

<TermsPage lang="en" />
